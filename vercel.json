{
  "buildCommand": "cd printquote && npm install && npm run build",
  "outputDirectory": "printquote/.next",
  "installCommand": "cd printquote && npm install",
  "framework": "nextjs",
  "rootDirectory": "printquote"
}
