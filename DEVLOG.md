# 開発ログ — Ma Bibliothèque（書籍購入・発売スケジュール管理アプリ、旧: 漫画発売リスト）

このファイルは「何を作ったか」だけでなく「何を考えてそう作ったか」を残すための記録です。gitのコミット履歴は"変更内容"は追えますが"理由"は残らないため、このログで補完します。新しいセッションを始めるときは、まずこのファイルを読むと経緯が分かります。

対象ファイル: `manga.html`（単一HTMLファイル構成、Firebase Auth + Realtime Database）
公開URL: https://kenny-s-52241.web.app/bibliotheque

日々の詳細な作業ログは同じディレクトリの `SESSION_YYYYMMDD.md` に残しているので、コミットハッシュや技術的なTipsが欲しい場合はそちらを参照。このDEVLOGは意思決定の経緯・理由に絞って書く。

---

## 〜2026-07-10: 価格データの信頼性問題

自動入力した価格が実売価格と食い違う（例: 実売792円のところ800円になる）という報告を起点に、価格・出版社取得まわりの情報源を総点検した。版元ドットコム(hanmoto) APIが完全にダウン、OpenBDのタイトル検索エンドポイントはそもそも存在しない、楽天Books APIは旧エンドポイント(`app.rakuten.co.jp`)が廃止され新方式(`openapi.rakuten.co.jp` + `accessKey`必須)に未対応、という状態が重なり、結果的に電子書籍版価格（iTunes）にフォールバックしていたのが誤価格の直接原因だった。楽天Books APIを新方式に移行し復旧させたことで、紙版の正しい価格・出版社が取れるようになった。

この一件から、「情報源が複数あっても、実際に動いているか定期的に確認しないと気づかないうちに劣化する」ことを学び、以後は新機能を足すたびに実データでcurl検証する習慣がついた。

---

## 2026-08-21: 機能拡充とセキュリティ監査

### 単発の機能追加ではなく、監査からの気づきで方針が変わった日
この日の最大の出来事は、ユーザーから「他人が個人のGoogleアカウントでログインして使うとき、APIキーの扱いはどうなっているか」という素朴な質問を受けたこと。この質問をきっかけに実際のFirebase設定を監査したところ、2つの問題が見つかった。

1. Firebase Hostingの`ignore`リストに漏れがあり、無関係な`stock-portfolio`・`app/`ディレクトリのソースコードが実際に公開されていた。
2. Realtime Databaseの`public_catalog`/`public_config`パスが、認証済みユーザーなら誰でも書き換え可能になっていた。表示先の`manga-public.html`も既に消えており（404）、機能として使われていないのに書き込み穴だけが残っている中途半端に危険な状態だった。

「公開する」の意味をユーザーに確認したところ、想定は「アプリ自体を配って各自が自分のデータを管理する」であり、ユーザー間でデータを見せ合うソーシャル機能は不要と判明。そのため`public_catalog`機能自体を丸ごと削除し、該当パスを`.write:false`に締める方針にした。**「使われていない機能を治すより消す」を選んだのは、修正コストと再発リスクを考えた結果。**

### 媒体タイプ拡張（漫画専用→書籍全般へ）
「漫画だけでなく小説・単行本・新書・文庫にも対応したい。別アプリにすべきか、この延長でやるべきか」とユーザーに意見を求められ、延長線上での実装を提案し合意を得た。理由は、価格・出版社取得、タグ、レビュー、五十音順ソート、発見機能など、既存の仕組みがそもそも「本」全般に通用する作りだったため。進め方は「まず小さく」（媒体タイプの分類フィールドだけ先行追加）を選んだが、後日（同日中）自動検索側6箇所が`booksGenreId=001001`（コミック限定）のまま残っているバグが見つかり、修正するまで小説検索が実質機能していなかった。**「小さく始める」際は、影響範囲（この場合は検索ロジック全体）を洗い出すチェックリストを作ってから着手すべきだった、という反省点。**

### 他セッションとのgit競合事故
`git add manga.html && git commit`で、並行して動いていた別セッション（stock-portfolio作業中）がステージ済みだった無関係な5ファイルを巻き込んでコミットしてしまった。実害はなく相手セッションとも合意の上で収束したが、**以後このリポジトリ（複数プロジェクトが同居する共有ワークスペース）で作業する際は、`git add`前に必ず`git status`でステージ内容を確認するルールを徹底することにした。**

---

## 2026-08-22: URL・ブランディング刷新

書籍全般に対応した流れで、「漫画発売リスト」という名前も実態に合わなくなってきた。ユーザーに複数の候補名を出す中で「フランス語にしてみて」というリクエストがあり、"Ma Bibliothèque"（私の書庫）に決定。URLも`/manga`から`/bibliotheque`に変更したが、旧URLへのブックマークが死なないよう301リダイレクトを設定。ランチャーページのリンク・表示名も合わせて更新。

インターフェースの配色も、フランス語タイトルの温かみに合わせてパープル系からテラコッタ・アンバー調に刷新。ハードコードされていた紫系カラーが約45箇所あり、全てCSS変数参照に統一する地味だが重要なリファクタリングを行った。**「見た目を変える」だけの依頼に見えても、既存コードがどれだけ値をハードコードしているかによって作業量が大きく変わる、という教訓。**

---

## 2026-08-23: 購入者目線でのUI大改善、そして「表示が変わらない」問題の深掘り

### 未購入リストの導入と、表示順「もっと柔軟に」の連鎖
「買おうと思ったリストが最初のページにないと不便」という指摘から、未購入分（発売日が過去でも）を常に見える形で表示する機能を追加。ここから表示位置についての要望が「下に→発売日未定より上に→もっと柔軟に→発売リストも固定じゃなくていい」と4段階でエスカレートし、最終的に3セクションを▲▼で自由に並べ替えられるUIに落ち着いた。**毎回コードを直接書き換えて対応していたが、3回目の要望が来た時点で「これは設定化すべきパターンだ」と気づくべきだった。表示順・見た目の好みに関わる要望は、最初から設定でカスタマイズ可能にしておく方が結果的に早い。**

### 表紙画像とライトボックス、そして「大きくならない」の真因探し
月別リスト等に表紙サムネイルを追加し、クリックで拡大表示するライトボックス機能を実装。ここでユーザーから「画像の表示はもっと大きくなりませんか？」と指摘され、対応が3段階になった。

1. まず「サムネイルは楽天CDNの低解像度(`_ex=200x200`)を使い回している」と判断し、拡大表示時だけ高解像度(`_ex=600x600`)のURLに差し替え。実データで解像度が確かに上がることを確認したが、ユーザーからは「変わって見えない」との再指摘。
2. 再調査の結果、真因は解像度ではなく表示サイズのCSSにあった。`max-width:90vw;max-height:90vh`は「画像の元のピクセルサイズ」を上限にするだけで、それ以上には拡大しない仕様だったため、大画面PCほど相対的に小さく見えていた。`width:90vw;height:90vh;object-fit:contain`に変更し、常に画面の9割を占めるサイズまで拡大表示するよう修正。
3. さらに調査の過程で、`manga.html`が`Cache-Control: max-age=3600`のためデプロイ後も最大1時間ブラウザに古い版がキャッシュされ続ける問題を発見。これが「直したのに反映されていないように見える」原因の一つだったと判明し、`no-cache`（毎回サーバーへ確認）に変更した。

**この一連の流れから得た教訓は2つ。①「もっと大きく」のような曖昧な要望は、原因の仮説を1つ実装して終わりにせず、ユーザーの再指摘を真摯に深掘りする（今回は解像度の問題だと思ったら表示サイズの問題だった）。②「直したのに変わらない」という報告を受けたら、まずキャッシュを疑う。単一HTMLファイル構成でキャッシュバスティングの仕組みがないアプリでは、デプロイ直後の見た目が古いままという事態が構造的に起きやすい。**

---

## 使い方（このログの運用ルール）

- 大きな機能追加・仕様変更・重要なバグ修正・ユーザーとの重要なやり取り（方針転換につながった指摘など）があったら、その日付の下に追記する
- 「何を」より「なぜそうしたか」「どんな制約・失敗があったか」を優先して書く（コードの詳細はgit差分や`SESSION_YYYYMMDD.md`を見ればわかるため）
- 次のセッションを始めるときは、このファイルの最新エントリから読むと経緯を素早く追える
