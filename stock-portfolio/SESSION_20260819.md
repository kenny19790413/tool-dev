# StockPortfolio 作業記録 2026-08-19

## 全体ステータス

Claude Codeの権限プロンプト削減設定を行った後、証券会社CSVインポート機能を新規実装。当初は岡三証券の「取引履歴」CSVのみを想定していたが、実データを見ながら進める中でスコープが拡大し、最終的に「取引履歴CSV」と「保有残高CSV」の2種類を自動判別して取り込める機能になった。実データ（岡三証券・みずほ証券）でパース→既存資産との突合→反映までを検証済み。

その後、8/17記録の持ち越しタスク「1. 年間分配金額の反映」に着手し、実際に岡三証券・みずほ証券の取引履歴CSVを取り込んで4件の資産に反映。あわせて、ユーザーから提供された分配方針の実データ（Web検索結果のスクリーンショット2件）をもとに、ダッシュボードの未入力警告ロジックを「決算月が判明していても、まだその月を迎えていない資産は警告しない」よう改善。最終的にプライベート資産の分配金未入力警告は0件になった。コミット`5124bf4`（CSV機能）・`b200d4a`（警告ロジック改善）としてpush済み。

## 今回やったこと

### 1. Claude Codeの権限プロンプト削減（fewer-permission-prompts）

ターミナルで`claude --dangerously-skip-permissions`を使う様子をユーザーが共有してくれたのをきっかけに、「このセッションでも確認を減らせないか」との相談を受けた。CLIの起動オプションのようにセッション途中で権限モード自体を切り替えることはできない旨を説明した上で、`fewer-permission-prompts`スキルを提案・実行。

- 直近14セッション分のトランスクリプトを解析し、Bash/MCPツール呼び出しの頻度を集計
- `dejiina_agent/.claude/settings.json`を新規作成し、読み取り専用の9パターンをallowlistに追加（`mcp__Claude_Browser__navigate`等のブラウザ読み取り系が中心、`Bash(tasklist *)`のみBash側）
- `curl`・`npx`・`node -e`・`powershell -Command`など、任意コード実行や書き込みを伴う可能性のあるコマンドは意図的に除外（判断根拠も含めユーザーに説明済み）

### 2. 証券会社CSVインポート機能の実装

**きっかけ**: 「証券会社の取引履歴やCSVを読み込んで登録していくことは可能か」との要望。取引履歴CSVと保有残高CSVの違い・実装コストの差を説明した上で、まず「取引履歴CSV」を選んでもらい、岡三証券の実サンプルを提供してもらって仕様を確定した。

**当初実装**（岡三証券の取引履歴CSV専用）
- `lib/stock/csvImport.ts`: CSV行パーサー、銘柄名の近似マッチング（`matchAsset`）、購入・分配金の集計ロジック
- `app/api/import/csv/route.ts`: アップロードされたCSVを解析し、既存資産との突合結果を返すAPI
- `app/assets/import-csv/page.tsx`: プレビュー画面（分配金は編集して反映、購入取引は参考情報表示のみ）
- 検証中に、外投（USD建て資産）にも投資信託特有の「単価は1万口あたり」という換算を誤って適用していたバグを発見し修正（購入額が実際の1/10000になっていた）

**スコープ拡大**（ユーザーがみずほ証券CSV・岡三証券の別CSV・野村証券PDF・スクリーンショット画像を追加提供）
- みずほ証券のCSVは岡三証券と列構成が完全に同一と判明。共通パーサーでそのまま対応
- 岡三証券の新しいCSVは実は「取引履歴」ではなく「預り資産（預り証券）」＝保有残高CSVだった。こちらは決算日欄（例:「毎月/25」「06/15・12/15」「無分配」）から分配月を高精度に推定でき、当初想定していた取引履歴CSVより価値が高いデータだったため、`lib/stock/csvHoldings.ts`を新設して対応を追加
- 野村証券PDFは帳票形式でテキスト抽出が崩れやすく構造化が難しいため、今回は対応を見送り。同時に提供されたスクリーンショット画像は既存の「画像から追加」機能で代替可能と案内
- 銘柄名の名寄せで、全角/半角の表記揺れ（「ＪＰモルガン」/「JPモルガン」）と、証券会社側の名前末尾切れ（文字数制限によるトランケート）・DB側の証券コードサフィックス付与という2種類の不一致が判明。`normalizeName`にNFKC正規化を追加し、完全一致で見つからない場合は正規化後の最長共通接頭辞でファジーマッチングする方式に変更
- CSV種別（取引履歴/保有残高）はヘッダー行の文字列で自動判別し、`app/api/import/csv/route.ts`が適切なパーサーに振り分ける設計に変更

**検証**
- 実際の岡三証券「保有残高」CSVをブラウザのファイル入力に直接セットして解析→プレビュー表示→「反映する」ボタンをクリックする一連の操作を実地確認
- 5件のPRIVATE資産（ダイワ・ブラックストーンF、ブラックロックPEF、ニュートン、ピクテ、日本ニューテクノロジー・オープン）の評価額（Valuation新規記録）・分配月が正しくDBに反映されたことをAPI経由で確認
- ダッシュボードの総資産額（¥277,157,181 → ¥277,550,656）・資産クラス別内訳に反映されることを確認
- 「配当・分配金が未入力」警告は意図通り変化なし（保有残高CSVには金額情報がなく、月のみ反映する設計のため）

### 3. 年間分配金額の反映（8/17記録の持ち越しタスク1番）

保有残高CSVには金額情報がないため、実際に岡三証券・みずほ証券の「取引履歴」CSVをブラウザのファイル入力に直接セットして取り込み、以下を反映した。

- **ピクテ・グローバル・インカム株式ファンド**: 年間988,076円（CSV期間3ヶ月分から月平均×12で算出）
- **ダイワ・ブラックストーン・プライベート・クレジットF**: 年間$3,252
- **日本ニューテクノロジー・オープン**: 年間1,092,300円
- **新・ミューズニッチ米国BDCファンド**: 年間2,472,528円
- **ブラックロック・プライベート・エクイティ・ファンド**: 保有残高CSVで「無分配」と確認済みだったため、`annualDistribution=0`を明示的に設定（CSVインポート機能の対象外のため直接PATCH）

反映後、ダッシュボードの未入力警告は6件→2件（JPモルガン・ニュートン）に減少。年間配当・分配金見込みは¥5,195,780→¥10,145,963に更新。

### 4. 個別ファンドの分配方針をユーザー提供情報で補完

- **ニュートン・パワー・イノベーション・ファンド**: ユーザーが検索結果のスクリーンショットで「決算月10月・毎年10月21日」を確認。既に保有残高CSVから同じ内容が反映済みであることをAPI経由で裏付け確認（追加変更なし）
- **JPモルガン・米国テクノロジー株式ファンド**: 同様にスクリーンショットで「年1回・4/20決算、支払い/金額とも保証なし」という分配方針を確認。`distributionMonths=[4]`を設定し、方針をnoteに記録（金額は不明のためannualDistributionはnullのまま維持）
- その後ユーザーから、このファンドが**2026年7月31日設定の新規ファンド**（愛称: ザ・テック・リーダーズ）であり、初回決算は2027年4月、今年（2026年4月）時点では分配実績自体が存在しないとの情報提供を受けた。`annualDistribution=0`（実績なし、を明示）に修正し、新規設定の経緯をnoteに追記

### 5. ダッシュボード警告ロジックの改善（決算月未到来の資産を警告対象から除外）

JPモルガンの分配方針確認をきっかけに、「ニュートンは決算月（10月）がまだ先なのに、ダッシュボードでは他の未入力資産と同列に警告されるのはおかしい」という指摘を受けた。`lib/portfolio.ts`に`isDistributionInfoOverdue(asset, today)`を新設し、`app/page.tsx`の警告フィルタを差し替え。

- 決算月（`distributionMonths`）が判明していない資産 → 従来通り常に警告
- 決算月が判明している資産 → 当年その月（のいずれか）を迎えるまでは警告しない（`distributionMonths.some(m => m <= currentMonth)`で判定）

警告カードの文言も「決算月を過ぎても配当・分配金が未入力のプライベート資産が◯件あります」に変更。実データで、ニュートン（10月・未到来）が警告から外れ、JPモルガン（4月・既に経過）が残ることを確認してから修正・pushした（コミット`b200d4a`）。

## 開発中に踏んだハマりどころ（技術メモ）

### 証券会社CSVの「単価」は商品種別によって意味が違う
投資信託（岡三証券CSVでの商品区分＝「投信」）の単価は「1万口あたりの基準価額」という業界慣習があるが、外国株式・外国投資（「外株」「外投」）の単価はそのまま1株/1口あたりの価格。同じ列でも商品区分によって割る数が変わるため、`productType === '投信'`の場合のみ10000で割るよう分岐が必要だった。

### 証券会社CSVの銘柄名は完全一致で照合できない
- 岡三証券CSVは銘柄名が長いと末尾が切れる（文字数制限によるトランケート）
- DB側の登録名には証券コード等のサフィックス（「（X6653）」等）が付いていることがある
- 全角/半角表記が証券会社によって異なる（「ＢＤＣ」/「BDC」等）

これらを吸収するため、NFKC正規化＋空白/括弧除去で正規化した上で、完全一致がなければ「正規化後の最長共通接頭辞が一定長以上」という基準でファジーマッチングする方式を採用。実データで全パターンの誤マッチ・取りこぼしがないことを確認済み。

### Node.jsの`TextDecoder`は`shift_jis`をネイティブサポートしている
証券会社CSVはShift-JISエンコードのことが多いが、今回提供されたファイルはいずれもUTF-8（BOM付き）だった。`TextDecoder('utf-8', {fatal:true})`が失敗したら`TextDecoder('shift_jis')`にフォールバックする実装にしており、どちらの形式でも動作する（BOMは`TextDecoder`が自動的に除去してくれる）。

## 次回やること

1. ~~年間分配金額（annualDistribution）の反映~~ — **完了**。プライベート資産の分配金未入力警告は0件
2. **野村証券PDFへの対応検討** — 帳票PDFの構造化方法（Gemini Vision経由でページを画像として解析する等）を検討するか、当面はスクリーンショット画像＋既存の画像インポートで代替するか方針を決める
3. **スクリーンショット画像（アスリナ・ちゅうぎんフィナンシャルグループ・トマト銀行・NTT）の取り込み** — 既存の「画像から追加」機能でそのまま対応できるはずなので、次回実際に試す
4. **旧パス（`C:\Users\kenny\OneDrive\Desktop\ツール開発`）の削除** — `dejiina_agent`フォルダが何らかのプロセスにロックされたままで削除できていない。OneDriveの同期停止や関連ウィンドウを閉じてから再試行
5. **`SESSION_20260817.md`からの持ち越し** — printquote・meishi-order等、stock-portfolio以外のプロジェクトも新パス（`C:\dev\tool-dev`）での動作確認がまだ済んでいない
6. **株式（STOCK）型資産のCSV反映** — 今回は参考情報表示のみ。DBのSTOCK資産と銘柄コードで正確に突合できれば、投信/外投と同様に保有数量・評価額の反映を検討する余地がある
7. **JPモルガンの初回決算（2027年4月）の確認リマインド** — `annualDistribution=0`のまま`isDistributionInfoOverdue`のチェックをすり抜ける設計にしているため、2027年4月以降に実績が出たら手動で確認・入力が必要（自動では気づけない）

## ローカル起動方法

```bash
cd C:\dev\tool-dev\stock-portfolio
npm install
npx prisma generate
npm run dev
```
